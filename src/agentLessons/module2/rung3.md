Purpose: Cretes the clear button and its logic

1. Where: In the calcskeleton director

2. logic: Create a button that will clear the display and set it back to 0. The components state should also
reset to 0. 

3. UI: Add a button to clear.
The button will render a green C when the button can clear the display. This happens when the user presses
a number button.
The button will render a black C when the state is empty, i.e. after the user hits clear the black C will
display.

The button will go on the top right. This is also a good time to seperate the buttons into divs. Display
the buttons, as divs, similar to a calculator might. That is a 3 x 3 matrix.

Green and black is the letter of the C, not the button.
4. Wiring: No wiring at this time between files.

5. Behavior: When a user clicks clear, the display is reset to 0.

Ignore invalidInput for now, we will circle back that if needed.


